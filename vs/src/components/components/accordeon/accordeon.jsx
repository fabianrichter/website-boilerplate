import React from "react";
import PropTypes from "prop-types";

const Accordeon = ({ data }) => {
  return (
    <section>
      <Container></Container>
    </section>
  );
};

Accordeon.propTypes = {};

export default Accordeon;
